{
  "name": "quick-memo",
  "version": "1.14.0",
  "description": "A CLI tool for quick note taking with tags and search",
  "main": "src/index.js",
  "bin": {
    "memo": "./bin/memo"
  },
  "scripts": {
    "test": "node tests/test.js && node tests/test-indexer.js && node tests/test-atomic.js && node tests/test-fuzzyCache.js && node tests/test-config-mtime.js",
    "start": "node bin/memo",
    "benchmark": "node benchmarks/search-benchmark.js",
    "benchmark:index": "node benchmarks/index-rebuild.js",
    "benchmark:ops": "node benchmarks/add-edit-delete.js",
    "benchmark:all": "npm run benchmark && npm run benchmark:index && npm run benchmark:ops"
  },
  "keywords": [
    "productivity",
    "notes",
    "cli"
  ],
  "author": "OpenClaw",
  "license": "MIT",
  "dependencies": {
    "chalk": "^4.1.2",
    "commander": "^11.0.0",
    "fs-extra": "^11.0.0",
    "ora": "^8.0.1",
    "string-similarity": "^4.0.4"
  },
  "engines": {
    "node": ">=14"
  },
  "devDependencies": {
    "fast-check": "^4.7.0"
  }
}
